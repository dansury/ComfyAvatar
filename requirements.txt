# --- Ядро веб-сервиса (обязательно) ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
requests>=2.31
websockets>=12.0
pydantic>=2.6

# --- ComfyUI взаимодействие ---
# (используется requests/websockets выше)

# --- TTS: Coqui XTTS v2 (клонирование голоса) ---
# torch ставится отдельно под вашу CUDA/CPU: https://pytorch.org/get-started/locally/
torch>=2.1
torchaudio>=2.1
coqui-tts>=0.24.0          # активный форк Coqui TTS (импортируется как `TTS`)
# transformers 5.x удалил функцию `isin_mps_friendly`, на которую опирается XTTS,
# из-за чего падает импорт: "cannot import name 'isin_mps_friendly'".
# Фиксируем ветку 4.x (последняя совместимая — 4.57.x).
transformers>=4.43,<5.0
soundfile>=0.12
numpy>=1.24

# --- TTS: Kokoro (опционально, лёгкий движок) ---
# kokoro>=0.7.0            # раскомментируйте, если хотите Kokoro
